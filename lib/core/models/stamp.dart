class Stamp {
  String unitName;
  String productName;
  String createAt;
  String expriedAt;
  int width;
  int height;

  Stamp(this.unitName, this.productName, this.createAt, this.expriedAt,
      this.width, this.height);
}
